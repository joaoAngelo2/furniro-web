import React from 'react';
/*import useProducts from '../hooks/useProducts';
import ProductCard from './ProductCard';

const ProductGrid: React.FC = () => {
  const { products, loading } = useProducts(); // sem categoria

  if (loading) {
    return <p className="text-center text-lg font-medium my-10">Carregando produtos...</p>;
  }

  if (products.length === 0) {
    return <p className="text-center text-lg font-medium my-10">Nenhum produto encontrado.</p>;
  }

  return (
    <div className="w-screen text-center">
      <p className="text-3xl font-bold text-zinc-800 mb-6">Our Products</p>
      <div className="w-4/5 mx-auto grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8">
        {products.slice(0, 8).map((product) => (
          <ProductCard key={product.id} product={product} />
        ))}
      </div>
    </div>
  );
};
*/
//export default ProductGrid;
